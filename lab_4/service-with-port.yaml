# service-with-port.yaml
apiVersion: v1
kind: Service
metadata:
  name: nginx-service
spec:
  selector:
    app: nginx
  ports:
  - port: 8080              # Service listens on port 8080
    targetPort: 80          # Forwards to pod port 80
