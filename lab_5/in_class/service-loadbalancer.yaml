apiVersion: v1
kind: Service
metadata:
  name: nginx-service
spec:
  type: LoadBalancer
  selector:
    app: fastapi-nginx-app
  ports:
  - port: 8080              # Service listens on port 8000
    targetPort: 80          # Forwards to pod port 80
